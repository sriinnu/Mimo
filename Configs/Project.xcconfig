// Project Metadata
PROJECT_NAME = Mimo
PRODUCT_BUNDLE_IDENTIFIER = com.sriinnu.Mimo
MARKETING_VERSION = 1.0.1
CURRENT_PROJECT_VERSION = 2
PROJECT_CREATOR = Sriinnu

// Include local signing config (not tracked in git)
#include? "Local.xcconfig"
